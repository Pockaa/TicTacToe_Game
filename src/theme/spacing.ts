/** 8px-based spacing scale used for padding, margins and gaps. */
export const spacing = {
    xs: 4,
    sm: 8,
    md: 12,
    lg: 16,
    xl: 24,
    xxl: 32,
    xxxl: 48,
} as const;

/** Corner-radius scale. `pill` rounds to a full capsule. */
export const radius = {
    sm: 10,
    md: 14,
    lg: 16,
    xl: 20,
    pill: 999,
} as const;
